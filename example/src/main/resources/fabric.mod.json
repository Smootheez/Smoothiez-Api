{
  "schemaVersion": 1,
  "id": "${modid}",
  "version": "${modVersion}",
  "name": "${modName}",
  "description": "${modDescription}",
  "authors": [
    "Smootheez"
  ],
  "license": "${modLicense}",
  "environment": "*",
  "entrypoints": {
    "client": [
      "io.github.smootheez.example.ExampleClient"
    ],
    "main": [
      "io.github.smootheez.example.Example"
    ],
    "smoothiezapi": [
      "io.github.smootheez.example.config.ExampleConfig"
    ]
  },
  "depends": {
    "fabricloader": ">=${loaderVersion}",
    "minecraft": ">=${minecraftVersion}"
  }
}